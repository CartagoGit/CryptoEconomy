// import { types } from "../types/types";

const plataform = { data: null, plataform_name: null, last_fetch: null };


const initialState = [plataform];

export const cryptos = (state = initialState, action) => {
	switch (action.type) {
		default:
			return state;
	}
};

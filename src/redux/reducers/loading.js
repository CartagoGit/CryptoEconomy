// import { types } from "../types/types";

const initialState = {
	loading: null,
	error: null,
	data_fetched: null,
};

export const loading = (state = initialState, action) => {
	switch (action.type) {
		default:
			return state;
	}
};

